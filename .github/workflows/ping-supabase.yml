name: Supabase Keep-Alive

on:
  schedule:
    - cron: '0 9 */4 * *'  # 4일마다 오전 9시 (UTC)
  workflow_dispatch:        # 수동 실행도 가능

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase
        run: |
          curl -s \
            -H "apikey: sb_publishable_VhU_zhguD5UkIaMqGdLQCQ_cBjQI3yG" \
            -H "Authorization: Bearer sb_publishable_VhU_zhguD5UkIaMqGdLQCQ_cBjQI3yG" \
            "https://mspwdasiewqtwfyngdhy.supabase.co/rest/v1/members?select=id&limit=1"
          echo "Pinged Supabase at $(date)"
